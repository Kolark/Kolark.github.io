---

---

<p class="contact_links">
    <a class="contact_link" href="">Email</a> -
    <a class="contact_link" href="">Github</a> -
    <a class="contact_link" href="">Linked In</a>
</p>

<style>
    .contact_links {
        color: var(--colorC);
    }
</style>

<script>
    import gsap from "gsap";

    gsap.fromTo(
        ".contact_link",
        {
            opacity: 0,
        },
        {
            opacity: 1,
            stagger: 0.15,
            duration: 0.75,
            ease: "power2.out", // Smooth easing
        }
    );
</script>
